{
  "name": "pcb-tracker-server",
  "version": "1.0.0",
  "description": "PCB Inventory Tracker — Backend API",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "node src/seed.js",
    "postinstall": "npx puppeteer browsers install chrome",
    "render-build": "npm install && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "express": "^4.21.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.14",
    "pg": "^8.13.0",
    "puppeteer": "^23.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
